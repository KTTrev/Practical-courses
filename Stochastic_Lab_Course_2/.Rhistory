install.packages("ggplot2")
install.packages("dplyr")
#remove all variables that start with “s“, “v” and “m”, followed by a number
children1<- children %>%
select(c(-starts_with('m33')))#, -starts_with('v0:9'), -starts_with('m0:9')))
#remove all variables that start with “s“, “v” and “m”, followed by a number
children1<- children %>%
select(c(-starts_with('m34')))#, -starts_with('v0:9'), -starts_with('m0:9')))
children
setwd("~/Stochastic_Lab/Stochastic_Lab_Course_2")
library("tidyverse")
library("haven")
##Question(a)
children <- read_dta("childrenfinal.dta")
#remove all variables that start with “s“, “v” and “m”, followed by a number
children1<- children %>%
select(c(-starts_with('m34')))#, -starts_with('v0:9'), -starts_with('m0:9')))
children1
#remove all variables that start with “s“, “v” and “m”, followed by a number
children1<- children %>%
select(c(-starts_with('m3')))#, -starts_with('v0:9'), -starts_with('m0:9')))
children1
#remove all variables that start with “s“, “v” and “m”, followed by a number
children1<- children %>%
select(c(-starts_with('v003')))#, -starts_with('v0:9'), -starts_with('m0:9')))
children1
#remove all variables that start with “s“, “v” and “m”, followed by a number
children1<- children %>%
select(c(-starts_with('v0')))#, -starts_with('v0:9'), -starts_with('m0:9')))
children1
str(children)
children[1,1]
#remove all variables that start with “s“, “v” and “m”, followed by a number
children1<- children %>%
select(c(-starts_with('v1')))#, -starts_with('v0:9'), -starts_with('m0:9')))
children
#remove all variables that start with “s“, “v” and “m”, followed by a number
children1<- children %>%
select(c(-starts_with('v0:1')))#, -starts_with('v0:9'), -starts_with('m0:9')))
children
#remove all variables that start with “s“, “v” and “m”, followed by a number
children1<- children %>%
select(c(-starts_with('v2')))#, -starts_with('v0:9'), -starts_with('m0:9')))
children
children1
#remove all variables that start with “s“, “v” and “m”, followed by a number
children1<- children %>%
select(c(-starts_with('v1')))#, -starts_with('v0:9'), -starts_with('m0:9')))
children1
#remove all variables that start with “s“, “v” and “m”, followed by a number
children1<- children %>%
select(c(-starts_with('v0:1')))#, -starts_with('v0:9'), -starts_with('m0:9')))
children1
#remove all variables that start with “s“, “v” and “m”, followed by a number
children1<- children %>%
select(c(-starts_with(c('v'))))#, -starts_with('v0:9'), -starts_with('m0:9')))
